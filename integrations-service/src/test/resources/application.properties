# Test profile - use default API keys for webhook auth
integrations.api-key.carrier=carrier-api-key-secret
integrations.api-key.processor=processor-api-key-secret
integrations.api-key.additional=other.api-key.additional
integrations.webhook.base-url=http://localhost:8081

# gRPC client for self-test (connect to our own server)
quarkus.grpc.clients.card-data.host=localhost
quarkus.grpc.clients.card-data.port=9100
