# Test profile - H2 in-memory, no Flyway, Hibernate creates schema
quarkus.datasource.db-kind=h2
quarkus.datasource.jdbc.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
quarkus.datasource.username=sa
quarkus.datasource.password=
quarkus.datasource.jdbc.max-size=2

quarkus.flyway.migrate-at-start=false
quarkus.hibernate-orm.database.generation=drop-and-create

# Kafka - dev services for tests
quarkus.kafka.devservices.enabled=true

# gRPC client for self-test (connect to our own server)
quarkus.grpc.clients.self.host=localhost
quarkus.grpc.clients.self.port=9002
